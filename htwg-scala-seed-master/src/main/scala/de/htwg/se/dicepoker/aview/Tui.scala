package de.htwg.se.dicepoker.aview

import de.htwg.se.dicepoker.controller.DPController

class Tui(controller: DPController) {
  def processInputLine(input: String): Unit = {
//    input match {
//      case "q" =>
//      case "n"=> DPController.createEmptyGrid(size)
//      case "r" => DPCcontroller.createRandomGrid(size, randomCells)
//      case "s" =>
//        val success= controller.solve
//        if (success) println("Puzzle solved")else println("This puzzle could not be solved!")
//      case _ => input.toList.filter(c => c != ' ').map(c => c.toString.toInt) match {
//          case row :: column :: value :: Nil => controller.set(row, column, value)
//          case _ =>
//        }
//
//    }
  }
}